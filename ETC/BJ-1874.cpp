// 우선 떨어질때 연속적이지 않은 경우, NO 먼저 체크하자

#include <iostream>
#include <stack>
#include <vector>

using namespace std;

bool chk[100010];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n; cin >> n;

    stack<int> s;
    vector<int> v;
    int past=0;
    int now;

    while(n--) {
        cin >> now;
        

        


        past = now;
    }


    return 0;
}