#include <iostream>
#include <vector>

using namespace std;

bool map[100][101];
int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int N;
    cin >> N;

    int x,y,d,g;

    while(N--) {
        cin >> x >> y >> d >> g;
        vector<pair<int, int>> v;        
        
        for (int i=0; i<=d; i++) {
            
        }

    }

    return 0;
}